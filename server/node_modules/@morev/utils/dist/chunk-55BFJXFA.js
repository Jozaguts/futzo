import {
  isNullish
} from "./chunk-Z5IVBZLV.js";

// src/objects/omit/omit.ts
var omit = (obj, ...keys) => Object.fromEntries(Object.entries(obj ?? {}).filter(([k]) => !keys.includes(k)));
var omitMutable = (obj, ...keys) => {
  if (isNullish(obj)) return obj;
  for (const key of keys) {
    delete obj[key];
  }
  return obj;
};

export {
  omit,
  omitMutable
};
