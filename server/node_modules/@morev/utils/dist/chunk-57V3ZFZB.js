import {
  toNumber
} from "./chunk-4MABVGSG.js";

// src/numbers/precision/precision.ts
var precision = (maybeNumber) => {
  const value = toNumber(maybeNumber, null);
  if (value === null || !Number.isFinite(value)) return null;
  let e = 1;
  let p = 0;
  while (Math.round(value * e) / e !== value) {
    e *= 10;
    p++;
  }
  return p;
};

export {
  precision
};
